server:
  port: 9005

spring:
  application:
    name: sct-order-hystrix-service
eureka:
  client:
    register-with-eureka: false
    service-url:
    #        设置与Eureka交互的服务地址
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
feign:
  hystrix:
    enabled: true
