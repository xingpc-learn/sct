server:
  port: 3377

spring:
  application:
    name: sct-nacos-config-service
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yaml

#${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}